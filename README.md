# mini_jwt-rbac-featureflags

- run test in Docker container with : `PYTHONPATH=/app pytest -v --tb=short tests/`
- run test in terminal with : `docker exec -e PYTHONPATH=/app mini_jwt-rbac-featureflags pytest -v --tb=short --color=yes tests/`